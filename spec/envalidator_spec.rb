require 'spec_helper'

describe Envalidator do
  it 'has a version number' do
    expect(Envalidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
